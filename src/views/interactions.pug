mixin interactionTable(list)
    table(class="table table-striped table-hover table-sm caption-top")
        caption
            =`Top interactions of @${handle} in the last 48 hours `
            | (sent 
            i(class="bi bi-box-arrow-up")
            | , received 
            i(class="bi bi-box-arrow-in-down")
            | )

        thead
            tr(style='text-align: center; vertical-align: middle')
                th(width='20px' rowspan=2) #
                th(rowspan=2) user 
                th(colspan=2) characters
                th(colspan=2) replies
                th(colspan=2) quotes
                th(colspan=2) reposts
                th(colspan=2) likes
                th(colspan=2) subtotal
                th(rowspan=2) total
            tr(style='text-align: center')
                th: i(class="bi bi-box-arrow-up")
                th: i(class="bi bi-box-arrow-in-down")
                th: i(class="bi bi-box-arrow-up")
                th: i(class="bi bi-box-arrow-in-down")
                th: i(class="bi bi-box-arrow-up")
                th: i(class="bi bi-box-arrow-in-down")
                th: i(class="bi bi-box-arrow-up")
                th: i(class="bi bi-box-arrow-in-down")
                th: i(class="bi bi-box-arrow-up")
                th: i(class="bi bi-box-arrow-in-down")
                th: i(class="bi bi-box-arrow-up")
                th: i(class="bi bi-box-arrow-in-down")
        tbody
            each val, idx in list
                tr(style='text-align: center; vertical-align: middle')
                    td= idx+1
                    td(style='text-align: left')
                        div(class='container text-left align-items-start')
                            div(class='row')
                                div(class='col-md-auto align-items-center' style="display: flex;")
                                    if val.avatar
                                        img(src=val.avatar width='32px' height='32px' style='border-radius: 20%;')
                                    else 
                                        i(class="bi bi-person-square" style="font-size: 2rem")
                                div(class='col align-items-center')
                                    div= val.displayName
                                    a(href='https://bsky.app/profile/'+val.did style="font-size: 0.8rem") #{(val.handle.length > 0) ? `@${val.handle}` : val.subject}
                    td= val.charactersGiven
                    td= val.charactersReceived
                    td= val.commentsGiven
                    td= val.commentsReceived
                    td= val.quotesGiven
                    td= val.quotesReceived
                    td= val.repostsGiven
                    td= val.repostsReceived
                    td= val.likesGiven
                    td= val.likesReceived
                    td= val.totalGiven
                    td= val.totalReceived
                    td= val.total

include layout.pug
    form(id="form" action="/interactions" method="get")
        input(id="handle" type="text" name="handle" value='' placeholder="User")
        input(id="button" type="submit" value="Search")

    p= textVal

    if userFound && interactions.length > 0
        +interactionTable(interactions)

                //- <Grid container alignItems="center">
                //-   <Grid sx={{ display: 'flex', width: 44 }}>
                //-     <Avatar sx={{ width: 25, height: 25 }} style={{ display: 'inline-block' }} src={option.avatar} />
                //-   </Grid>
                //-   <Grid sx={{ width: 'calc(100% - 44px)', wordWrap: 'break-word' }}>
                //-     <Typography variant="body2">
                //-       {option.displayName}
                //-       </Typography>
                //-     <Typography variant="caption" color="text.secondary">
                //-       @{option.handle}
                //-     </Typography>
                //-   </Grid>
                //- </Grid>